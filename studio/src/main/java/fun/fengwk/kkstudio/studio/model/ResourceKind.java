package fun.fengwk.kkstudio.studio.model;

/** Logical resource content family. Extended media types live in metadata. */
public enum ResourceKind {
  TEXT,
  IMAGE,
  VIDEO,
  AUDIO,
  DOCUMENT,
  JSON,
  BUNDLE
}
