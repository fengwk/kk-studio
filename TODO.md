# TODO 任务清单

- [x] 1. 扩充 `ai.ts` catalog，定义全部涉及的 i18n keys（zh-CN 与 en-US）
- [x] 2. 优化 CSS（`frontend/src/styles.css`）：
  - `.thread-debug-col-preview` 增加 `container-type: size;`
  - `.thread-system-prompt-body` 设置 `box-sizing: border-box; height: 50cqh; overflow-y: auto;` 并移除 `max-height: 115px;`
  - 添加复制按钮与复制反馈样式
- [x] 3. 重构 `ThreadModelRequestDebug.tsx`：
  - 顶部标题去掉 `DEBUG · ` 前缀，本地化为“下一次请求预览” / “Next Request Preview”
  - 环境 pill 本地化
  - “请求快照”按钮仅在 `debug.frozenInvocation` 存在时渲染，本地化文案与 title
  - 复制按钮移至 System Prompt 标题行旁，使用单一状态 `copyStatus ('idle' | 'copying' | 'copied' | 'error')`，避免永久显示已复制；`isMountedRef` 防止 unmount 后更新状态或设 timer；空 prompt 禁用；在 render 阶段调用 `t` 保证切 locale 即时响应
  - raw unknown delivery 保留原值，删除过期注释
  - Tools、Skills、Subagents、Cache、Badges title 本地化
- [x] 4. 重构 `ThreadDebugInspector.tsx`：
  - Inspector 各类标题、关闭按钮、字段行标题（Name, Status, Environment Support, Contributor, Description, Schema JSON, Skill Name, Package, Delivery, Path, Commits, Prompt XML 等）全面 i18n
  - 枚举本地化（Tool 状态、Filter 原因、Delivery、Cache Retention 无缓存映射、EnvironmentSupport 等）
  - 保留 raw 数据不变
- [x] 5. 重构 `ThreadEventView.tsx`：
  - 修复硬编码 `aria-label="Debug views"`，改为本地化 `tabsAriaLabel`
- [x] 6. 更新单测：
  - `ThreadModelRequestDebug.test.tsx`：覆盖中英真实切换、请求快照显隐、复制成功后立即重试失败、pending unmount resolve 无 timer、失败态切 locale、未知枚举 fallback 等
  - `ThreadEventView.test.tsx`：覆盖中英切换下的 tablist 与 tabs 本地化断言
- [x] 7. 更新 Playwright 测试与测试 harness：
  - `debug-harness.tsx` 字段对齐（requestJson）
  - `debug-responsive-layout.pw.ts`：更新高度比断言（近 0.5），新增 681-1100 动态 resize 高度比验证，增加截图 `debug-polish-wide.png` 和 `debug-polish-narrow.png`
- [x] 8. 更新 `docs/modules/frontend.md`：
  - 同步 50% 容器查询与内部滚动，移除 115px 失效描述
  - 同步顶部标题与请求快照入口说明
- [x] 9. 执行门禁与自测：
  - `npm test`（210 files, 2028 passed）
  - `npm run lint`（PASS, 0 errors, 0 warnings）
  - `npm run build`（PASS, tsc -b && vite build）
  - `npm run test:layout`（PASS, 34 passed）
  - 核心文件 coverage 检查（`ThreadModelRequestDebug.tsx` lines 94.36%, branches 90.36%, functions 95%）
  - 文档质量门禁（PASS, 23 fixed documents）与敏感数据门禁（PASS）
- [x] 10. 更新 NOTES/TODO 并提交 commit（不 push）
