Report the outcome of the current branch goal as `complete` or `blocked`, with a required non-empty `reason` containing detailed rationale and concrete evidence: for `complete`, the satisfied requirements and supporting evidence; for `blocked`, the repeated blocker, the attempts made, and the user input or external change required.

This only records your declaration against the current user goal. It never changes the goal text, never clears the goal, and never changes issue or business state. Set `complete` only when current evidence proves every requirement is satisfied and no required work remains. Set `blocked` only when the same blocking condition has repeated and meaningful progress cannot continue without user input or an external-state change. If the current goal already has a terminal report, wait for the user to change or clear the goal instead of reporting again.
