{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "File, directory, or supported image path to read."
    },
    "workdir": {
      "type": "string",
      "description": "Absolute directory on the target daemon's file system that this call runs in. Required on every call; it is never inherited from a previous call, a session default, or the environment root. Provide an expanded absolute path (no '~', no environment variable placeholders); Windows targets use a drive-rooted or UNC path."
    },
    "offset": {
      "type": "integer",
      "description": "Positive integer 1-based line offset for text reads. Defaults to 1."
    },
    "limit": {
      "type": "integer",
      "description": "Positive integer maximum number of text lines to return. Defaults to 200 and must not exceed 2000. When column_offset is specified, limit defaults to 1 and must equal 1."
    },
    "column_offset": {
      "type": "integer",
      "description": "Optional positive integer 1-based offset in Unicode code points within the selected text line. Text files only. When specified, limit defaults to 1 and must equal 1."
    }
  },
  "required": [
    "path",
    "workdir"
  ],
  "additionalProperties": false
}
