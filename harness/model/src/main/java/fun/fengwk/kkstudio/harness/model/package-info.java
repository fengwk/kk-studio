/**
 * 模型与 Provider 的无状态公共契约。
 *
 * <p>本包只描述模型、请求、流和用量；不依赖 Tool、Session、Runtime、Spring 或持久化框架。 Provider SDK 类型只能保留在 {@code
 * provider.adapter} 的实现内部，不能出现在本包公开 API。
 */
package fun.fengwk.kkstudio.harness.model;
