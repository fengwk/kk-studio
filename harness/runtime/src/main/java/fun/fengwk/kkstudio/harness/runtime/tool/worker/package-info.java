/**
 * Location-agnostic durable Tool worker. This package owns claim, lease, heartbeat, partial, retry,
 * terminal, resource externalization and Thread wake; local and remote Tool implementations only
 * execute.
 */
package fun.fengwk.kkstudio.harness.runtime.tool.worker;
