/**
 * Durable subagent task contracts. The task relation is keyed by parent tool invocation, while a
 * child remains an ordinary Session and Run that survives process recovery and cancellation.
 */
package fun.fengwk.kkstudio.harness.runtime.task;
