/**
 * Workspace Runtime 的领域边界。
 *
 * <p>本模块后续承载 Session Tree、Context、Durable Run、Tool Invocation 与权限策略； 它只依赖 harness-agent，不依赖
 * Spring、MyBatis 或 HTTP adapter。
 */
package fun.fengwk.kkstudio.harness.runtime;
