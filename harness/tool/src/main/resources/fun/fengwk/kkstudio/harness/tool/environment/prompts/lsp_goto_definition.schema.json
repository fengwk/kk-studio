{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Existing source file path supported by an LSP server. This path is also used to infer the workspace root."
    },
    "workdir": {
      "type": "string",
      "description": "Working directory for resolving relative paths. Defaults to the agent's current working directory. If provided, relative paths resolve from that directory."
    },
    "line": {
      "type": "integer",
      "description": "1-based line number for the target position."
    },
    "character": {
      "type": "integer",
      "description": "0-based character offset at the target position. Defaults to 0."
    }
  },
  "required": ["path", "line"],
  "additionalProperties": false
}
