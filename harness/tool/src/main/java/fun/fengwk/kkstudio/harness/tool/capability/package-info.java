/**
 * 独立于模型 Tool 的 Environment Capability 描述、参数、传输端口和流式执行 SPI。
 *
 * <p>本包只定义 codec-independent 的执行边界和 transport port，不包含 codec、registry 或 Daemon adapter 实现。
 */
package fun.fengwk.kkstudio.harness.tool.capability;
