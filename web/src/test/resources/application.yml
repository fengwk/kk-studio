spring:

  application:
    name: kk-studio

  sql:
    init:
      mode: never

  datasource:
    multi:
      primary:
        primary: true
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://127.0.0.1:1/unused
        username: unused
        password: unused

  data:
    redis:
      host: 127.0.0.1
      port: 6379

  mybatis:
    mapper-locations: classpath:**/*Mapper.xml
    type-aliases-package: fun.fengwk.kkstudio.**.model
    configuration:
      map-underscore-to-camel-case: true

convention:
  snowflake-id:
    initial-timestamp: 1702214261706
    # 测试固定 worker，避免无 Redis 时 worker 续租失败；生产见 application.yml Redis worker 策略。
    worker-id: 0

kk-studio:
  harness:
    runtime:
      workers-enabled: false
    environment-gateway:
      daemon-token: test-daemon-token
