{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "CFS canonical absolute path to create or replace."
    },
    "content": {
      "type": "string",
      "description": "Complete text file content."
    },
    "expected_revision": {
      "type": "integer",
      "description": "Expected revision for CAS: 0 to create new file, N (>0) to replace current revision N."
    }
  },
  "required": [
    "path",
    "content",
    "expected_revision"
  ],
  "additionalProperties": false
}
