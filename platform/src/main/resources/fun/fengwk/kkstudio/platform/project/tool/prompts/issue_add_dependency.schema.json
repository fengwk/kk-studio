{
  "type": "object",
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "Target issue that depends on another issue."
    },
    "depends_on_issue_id": {
      "type": "string",
      "description": "Prerequisite issue that must be completed first."
    },
    "expected_version": {
      "type": "integer",
      "description": "Expected current version of the target issue for optimistic concurrency control."
    }
  },
  "required": [
    "issue_id",
    "depends_on_issue_id",
    "expected_version"
  ],
  "additionalProperties": false
}
