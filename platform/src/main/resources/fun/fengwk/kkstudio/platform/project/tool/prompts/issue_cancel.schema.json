{
  "type": "object",
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "ID of the issue to cancel."
    },
    "expected_version": {
      "type": "integer",
      "description": "Expected current version of the issue for optimistic concurrency control."
    },
    "reason": {
      "type": "string",
      "description": "Optional cancellation reason."
    }
  },
  "required": [
    "issue_id",
    "expected_version"
  ],
  "additionalProperties": false
}
