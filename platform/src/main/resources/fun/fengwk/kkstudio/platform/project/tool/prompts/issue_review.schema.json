{
  "type": "object",
  "properties": {
    "decision": {
      "type": "string",
      "enum": [
        "APPROVE",
        "REQUEST_CHANGES"
      ],
      "description": "Review decision: APPROVE or REQUEST_CHANGES."
    },
    "reason": {
      "type": "string",
      "description": "Review reasoning, findings, or required changes."
    }
  },
  "required": [
    "decision",
    "reason"
  ],
  "additionalProperties": false
}
