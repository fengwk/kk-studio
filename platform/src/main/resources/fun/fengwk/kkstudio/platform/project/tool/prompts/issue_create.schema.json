{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the issue."
    },
    "description": {
      "type": "string",
      "description": "Detailed description and requirements of the issue."
    },
    "assignee_agent_name": {
      "type": "string",
      "description": "Optional agent name assigned to execute the issue."
    },
    "reviewer_agent_name": {
      "type": "string",
      "description": "Optional agent name assigned to review the issue."
    },
    "initial_status": {
      "type": "string",
      "enum": [
        "BACKLOG",
        "TODO"
      ],
      "description": "Initial status of the issue. Defaults to BACKLOG. Only BACKLOG or TODO is allowed."
    }
  },
  "required": [
    "title"
  ],
  "additionalProperties": false
}
