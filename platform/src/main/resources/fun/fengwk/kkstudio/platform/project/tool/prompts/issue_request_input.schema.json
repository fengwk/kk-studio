{
  "type": "object",
  "properties": {
    "question": {
      "type": "string",
      "description": "Question or input requested from the user."
    },
    "context": {
      "type": "string",
      "description": "Optional context or background explanation for the request."
    }
  },
  "required": [
    "question"
  ],
  "additionalProperties": false
}
