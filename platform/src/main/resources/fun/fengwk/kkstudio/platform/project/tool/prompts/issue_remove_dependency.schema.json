{
  "type": "object",
  "properties": {
    "issue_id": {
      "type": "string",
      "description": "Target issue to remove the dependency from."
    },
    "depends_on_issue_id": {
      "type": "string",
      "description": "Prerequisite issue dependency to remove."
    },
    "expected_version": {
      "type": "integer",
      "description": "Expected current version of the target issue for optimistic concurrency control."
    }
  },
  "required": [
    "issue_id",
    "depends_on_issue_id",
    "expected_version"
  ],
  "additionalProperties": false
}
