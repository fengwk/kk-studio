name: kk-studio-canvas-test

# This stack is intentionally local-only:
# - host ports bind to 127.0.0.1;
# - services share a dedicated project network, isolated from other stacks;
# - all credentials below are fixed, disposable test values.
services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: canvas_test
      POSTGRES_USER: canvas_test
      POSTGRES_PASSWORD: canvas_test_only
    ports:
      - "127.0.0.1:${CANVAS_TEST_PG_PORT:-15432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U canvas_test -d canvas_test"]
      interval: 2s
      timeout: 3s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - canvas-test

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: canvas-test
      MINIO_ROOT_PASSWORD: canvas-test-only
    ports:
      - "127.0.0.1:${CANVAS_TEST_MINIO_PORT:-19000}:9000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:9000/minio/health/live"]
      interval: 2s
      timeout: 3s
      retries: 30
    volumes:
      - minio-data:/data
    networks:
      - canvas-test

  minio-init:
    image: minio/mc:RELEASE.2025-04-16T18-13-26Z
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: canvas-test
      MINIO_ROOT_PASSWORD: canvas-test-only
      MINIO_BUCKET: canvas-test
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        mc alias set local http://minio:9000 "$${MINIO_ROOT_USER}" "$${MINIO_ROOT_PASSWORD}"
        mc mb --ignore-existing "local/$${MINIO_BUCKET}"
        mc anonymous set none "local/$${MINIO_BUCKET}"
        mc stat "local/$${MINIO_BUCKET}"
        exec tail -f /dev/null
    healthcheck:
      test: ["CMD-SHELL", "mc stat \"local/$${MINIO_BUCKET}\" >/dev/null"]
      interval: 2s
      timeout: 3s
      retries: 30
    networks:
      - canvas-test

  http-mock:
    image: python:3.12-alpine
    init: true
    read_only: true
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
    command:
      [
        "python",
        "/mock/server.py",
        "--routes",
        "/config/routes.json",
        "--opencli-fake-fixtures",
        "/fixtures"
      ]
    ports:
      - "127.0.0.1:${CANVAS_TEST_MOCK_PORT:-18089}:8080"
    volumes:
      - ./mock/server.py:/mock/server.py:ro
      - ${CANVAS_TEST_MOCK_ROUTES:-./mock/routes.json}:/config/routes.json:ro
      - ../../core/src/test/resources/fun/fengwk/kkstudio/core/studio/resource:/fixtures:ro
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen("http://127.0.0.1:8080/health", timeout=2).read()
      interval: 2s
      timeout: 3s
      retries: 30
    networks:
      canvas-test:
        aliases:
          - opencli-hub
          - comfyui

  app:
    profiles: ["app"]
    build:
      context: ../..
      dockerfile: deploy/local/Dockerfile
    image: ${CANVAS_TEST_APP_IMAGE:-kk-studio-app:canvas-test}
    init: true
    environment:
      SPRING_PROFILES_ACTIVE: dev
      KK_STUDIO_DB_URL: jdbc:postgresql://postgres:5432/canvas_test
      KK_STUDIO_DB_USER: canvas_test
      KK_STUDIO_DB_PASSWORD: canvas_test_only
      MANAGEMENT_HEALTH_REDIS_ENABLED: "false"
      KK_STUDIO_STORAGE_S3_ENABLED: "true"
      KK_STUDIO_STORAGE_S3_ENDPOINT: http://minio:9000
      KK_STUDIO_STORAGE_S3_PUBLIC_ENDPOINT: http://127.0.0.1:${CANVAS_TEST_MINIO_PORT:-19000}
      KK_STUDIO_STORAGE_S3_REGION: us-east-1
      KK_STUDIO_STORAGE_S3_BUCKET: canvas-test
      KK_STUDIO_STORAGE_S3_ACCESS_KEY: canvas-test
      KK_STUDIO_STORAGE_S3_SECRET_KEY: canvas-test-only
      KK_STUDIO_CANVAS_RESOURCE_FFPROBE_BINARY: ffprobe
      KK_STUDIO_CANVAS_RESOURCE_FFMPEG_BINARY: ffmpeg
      KK_STUDIO_CANVAS_RESOURCE_PROCESS_TIMEOUT: 30s
      KK_STUDIO_CANVAS_RESOURCE_THUMBNAIL_MAX_DIMENSION: "512"
      KK_STUDIO_CANVAS_RESOURCE_THUMBNAIL_QUALITY: "80"
      KK_STUDIO_CANVAS_RESOURCE_TEMP_DIR: /tmp
      KK_STUDIO_CANVAS_RESOURCE_UPLOAD_EXPIRY: 15m
      KK_STUDIO_CANVAS_FUNCTION_FAKE_ENABLED: "true"
      KK_STUDIO_CANVAS_FUNCTION_EXECUTOR_CORE_SIZE: "1"
      KK_STUDIO_CANVAS_FUNCTION_EXECUTOR_MAX_SIZE: "2"
      KK_STUDIO_CANVAS_FUNCTION_EXECUTOR_QUEUE_CAPACITY: "8"
      KK_STUDIO_OPENCLI_HUB_ENABLED: "true"
      KK_STUDIO_COMFYUI_ENABLED: "false"
      KK_STUDIO_COMFYUI_BASE_URL: http://comfyui:8080
      KK_STUDIO_OPENCLI_HUB_BASE_URL: http://opencli-hub:8080
      KK_STUDIO_CANVAS_FUNCTION_GPT_IMAGE_2_PAID_ENABLED: "true"
      KK_STUDIO_CANVAS_FUNCTION_SEEDANCE_ENABLED: "true"
      KK_STUDIO_CANVAS_FUNCTION_SEEDANCE_WORKSPACE_ID: mock-workspace
      KK_STUDIO_CANVAS_FUNCTION_SEEDANCE_STATUS_POLL_INTERVAL: 10ms
      KK_STUDIO_CANVAS_FUNCTION_SEEDANCE_MAX_WAIT: 30s
    ports:
      - "127.0.0.1:${CANVAS_TEST_APP_PORT:-18088}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      minio-init:
        condition: service_healthy
      http-mock:
        condition: service_healthy
    networks:
      - canvas-test

volumes:
  postgres-data:
  minio-data:

networks:
  canvas-test:
    driver: bridge
